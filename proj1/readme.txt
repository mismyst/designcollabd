each frames are div or flex or grid  (box model)
after that each text has its own styling 
the whole frame --> (landing page , common dashboard --> one html (their styling shd html element tag )) whole frame 
